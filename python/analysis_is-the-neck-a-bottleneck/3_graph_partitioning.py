#!/usr/bin/env python3
"""Approach 3 of the neck-as-bottleneck investigation: graph-cluster the
whole BANC connectome and see at what cluster count ``n`` the brain and
VNC get put into different clusters.

Hypothesis: if the neck connective is *the* signalling bottleneck, the
first cut (n=2) should already split brain from VNC. If brain + VNC
share a cluster at small ``n`` and only separate at large ``n``, the
bottleneck is less dominant than the anatomy suggests.

Placeholder: implementation pending — see ``README.md``.
"""

raise NotImplementedError

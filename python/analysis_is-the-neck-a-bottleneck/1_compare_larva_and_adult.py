#!/usr/bin/env python3
"""Approach 1 of the neck-as-bottleneck investigation: compare ANs/DNs in
adult versus larval *Drosophila*.

The larval CNS has a brain and a VNC but no physical neck connective,
so it serves as a no-bottleneck baseline. Comparing the fraction of
larval neurons that interconnect brain + VNC against the same fraction
in the adult BANC quantifies whether the adult neck imposes a real
signalling bottleneck.

Placeholder: implementation pending — see ``README.md`` for the planned
approach and data sources (``Yijie_larva_type_meta.csv``).
"""

raise NotImplementedError
